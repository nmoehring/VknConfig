{
    "tasks": [
        {
            "type": "shell",
            "label": "(Debug) CMake configure and build",
            "command": "${workspaceFolder}\\build_debug.bat ${relativeFileDirname}",
            "args": [],
            "options": {},
            "problemMatcher": [
                "$msCompile"
            ],
            "group": "build",
            "detail": "Call CMake to configure and build target in debug mode."
        }
    ],
    "version": "2.0.0"
}